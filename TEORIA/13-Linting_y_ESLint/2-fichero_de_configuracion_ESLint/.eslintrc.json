// ARCHIVO DE CONFIGURACIÓN DE REGLAS ESLINT

/*
vamos a la página de ESLint y entramos en configuración
https://eslint.org/docs/latest/user-guide/configuring/configuration-files

buscamos códigos de ejemplo y copiamos y pegamos alguno

Vamos a indicar que siempre haya que usar dobles comillas y que nunca haga
falta usar punto y coma Volvemos a la página y entramos a las reglas
*/

{
    "rules": {
        // Override our default settings just for this directory
        "eqeqeq": "warn",
        "strict": "off",
        // que siempre sean comillas dobles -> sino, que de aviso:
        "quotes": ["warn", "double"],
        // que siempre haya que poner punto y coma (semicolon) -> sino, error:
        "semi": ["error", "always"],
        // que de error cuando haya objetos con atributos o funciones iguales:
        "no-dupe-keys": "error"
    }
}